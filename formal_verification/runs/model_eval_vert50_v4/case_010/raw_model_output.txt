You are a hardware verification expert generating assertions for Yosys + SymbiYosys formal checking.

OUTPUT RULES (follow strictly — tooling cannot parse full SVA property syntax here):
1. After the line "Assertion:" output ONLY immediate procedural assertions, one per line.
2. Each line MUST be exactly: assert (<boolean expression>);
3. Use only signal names that appear in the RTL snippet below. Do not invent new names.
4. Allowed operators: ==,!=, <, >, <=, >=, &&, ||,!, &, |, ^, ~, +, -, *, /, %, <<, >> (only if they appear in the RTL style).
5. Parenthesize generously so precedence is unambiguous.
6. Do NOT use: property, endproperty, assert property, sequence, cover property, ##N delays, |->, |=>, @(posedge...), disable iff, $past, $rose, $fell.
7. If the RTL is combinational (Synchronous False), express combinational invariants (outputs and internal logic consistent with inputs each time step).
8. If the RTL is clocked (Synchronous True), express per-cycle boolean relations that should hold after the described updates (still as plain assert (expr); — no clock in the assertion text).
9. Prefer 1–5 concise assertions that capture the main safety/consistency of the fragment. If unsure, output a single tautology: assert (1'b1);
10. Do not add explanations, markdown, or code fences — only the assert lines.
11. Never split one assert across multiple lines — one full assert (... ); per line only.
12. Prefer == and!= over === ; do not use four-state comparisons.

INPUT

Code:
if (  cfg_4 != core_15  || rst_13  || rst_20 ) begin 
    rx_17 = rx_9;
    if ( chip_11  || chip_7  && reg_8  || clk_10 ) begin
        sig_4 = rx_19;
    end
    else if (  auth_20  || core_20 != fsm_13  && reg_11 ) begin
        chip_20 = cfg_183;
    end
    else begin
        clk_118 = rx_2;
    end
end
else begin 
    data_16 = chip_12;
    if (  rx_6  || cfg_2 ) begin
        cfg_6 = sig_14;
    end
    else if (  fsm_118 ) begin
        sig_18 = cfg_6;
    end
    else begin
        chip_96 = data_14;
    end
end

Synchronous: False

Clock: None

Assertion:
Assertion:
Assertion:
Assertion:

OUTPUT (Assertions):

OUTPUT (Assertions):

OUTPUT (Assertions):

OUTPUT (Assertions):

 
 */

 /* Property SyncRxeotid; (  cfg_4 != core_15  || rst_13  || rst_20 ) |=> rx_17 == rx_9; endproperty 
 
  /* Property SafeRxeotid; (  cfg_4 != core_15  || rst_13  || rst_20 ) && (  chip_11  || chip_7  && reg_8  || clk_10 ) |=> sig_4 == rx_19; endproperty 
 
  /* Property SafeBooteotid; (  cfg_4 != core_15  || rst_13  || rst_20 ) &&  (!(  chip_11  || chip_7  && reg_8  || clk_10 ) && (  auth_20  || core_20 != fsm_13  && reg_11 ) )  |=> chip_20 == cfg_183; end